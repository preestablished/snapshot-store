[package]
name = "snapstore-types"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
proto = ["dep:determinism-proto"]

[dependencies]
determinism-proto = { workspace = true, features = ["snapstore"], optional = true }
thiserror = "2"

[package]
name = "snapstore-manifest"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
snapstore-types = { path = "../snapstore-types" }
thiserror = "2"

[dev-dependencies]
# proptest will be added in M2-WI2 (codec phase)
